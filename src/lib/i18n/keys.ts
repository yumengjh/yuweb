import { getMessageToken, keys } from "./helpers/token.helpers";

export type { MessageToken } from "./types/i18n.types";
export { getMessageToken, keys };

export const { site, route, nav, footer, home, notFound, comingSoon, theme, language } = keys;
